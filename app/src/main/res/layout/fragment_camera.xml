<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.camera.view.PreviewView
        android:id="@+id/viewFinder"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/capture_button_pager"
        android:layout_width="120dp"
        android:layout_height="100dp"
        android:layout_marginBottom="40dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ImageButton
        android:id="@+id/gallery_button"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginStart="20dp"
        android:background="@android:color/transparent"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_gallery"
        app:layout_constraintBottom_toBottomOf="@id/capture_button_pager"
        app:layout_constraintTop_toTopOf="@id/capture_button_pager"
        app:layout_constraintStart_toEndOf="@id/capture_button_pager" />

    <ImageButton
        android:id="@+id/switch_camera_button"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginEnd="20dp"
        android:background="@android:color/transparent"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_switch_camera"
        app:tint="@android:color/white"
        app:layout_constraintBottom_toBottomOf="@id/capture_button_pager"
        app:layout_constraintTop_toTopOf="@id/capture_button_pager"
        app:layout_constraintEnd_toStartOf="@id/capture_button_pager" />

</androidx.constraintlayout.widget.ConstraintLayout>
